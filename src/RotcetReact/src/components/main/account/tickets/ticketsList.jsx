import React from 'react'
import PropTypes from 'prop-types'

const TicketsList = props => {
    return (
        <div className='account__tickets_list'>
            
        </div>
    )
}

TicketsList.propTypes = {
    tickets: PropTypes.array.isRequired
}

export default TicketsList
